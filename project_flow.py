# to use CRUD operations on a mysql database using Flask.
# it provides endpoints to create, read, update, and delete records in specified tables.
# will use python main.py to run the flask app on localhost:5000
# and to upload csv file use project_flow.py with fastapi on localhost:8000
# it will provide an endpoint to upload csv or excel files to a specified table in the database.
# and it will sepataely handle the upload functionality.